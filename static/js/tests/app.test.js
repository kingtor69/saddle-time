const testWeatherObj = {
    
}
const testWeather HTML = `
`

describe('weater API tests', function() {
    it('should do stuff', function() {
        expect(updateWeatherDom(testWeatherObj).toEqual(testWeatherHTML))
    })
})